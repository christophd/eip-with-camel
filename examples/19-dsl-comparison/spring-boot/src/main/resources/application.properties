# Spring Boot: the same three concerns, different property namespaces.
spring.application.name=eip-dsl-comparison
server.port=8098

# No web starter here, so Camel's run controller is what keeps the JVM alive.
# Note the prefix: camel.main, not camel.springboot.
camel.main.run-controller=true

kafka.brokers=localhost:9092

logging.level.com.example.eip=INFO
