kafka.brokers=localhost:9092

quarkus.http.port=8083

# Disable background timer-driven routes during tests
eip.partitioned.producer.enabled=false
eip.consumer.lag.monitor.enabled=false

camel.main.shutdownTimeout=0
camel.component.kafka.shutdownTimeout=0
